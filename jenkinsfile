pipeline {
    agent {
        label 'SLAVE'
    }
    stages {
        stage('seeing compile code') {
            steps{
                sh '''
                mvn compile
                '''
            }
        }
        stage('package code') {
            steps {
                sh '''
                mvn package
                '''
            }
        }
        stage('seeing artifacts') {
            steps {
                sh '''
                cd target/users-api-0.0.1.jar  user.jar
                zip -r users.zip  user.jar*
                '''
            }
        }
    }
}