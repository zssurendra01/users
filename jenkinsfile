pipeline {
    agent {
        label 'SLAVE'
    }
    stages {
        stage('seeing compile code') {
            steps{
                sh '''
                mvn compile
                '''
            }
        }
        stage('package code') {
            steps {
                sh '''
                mvn pacakge
                '''
            }
        }
        stage('seeing artifacts') {
            steps {
                sh '''
                cd target/*.jar user.jar
                zip -r users.zip  user.jar*
                '''
            }
        }
    }
}