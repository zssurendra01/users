pipeline {
    agent {
        label 'SLAVE'
    }
    stages {
        stage('seeing compile code') {
            steps{
                sh '''
                mvn compile
                '''
            }
        }
        stage('package code') {
            steps {
                sh '''
                mvn package
                '''
            }
        }
        stage('seeing artifacts') {
            steps {
                sh '''
                cd target/*.jar user.jar
                zip -r user.zip  user.jar*
                '''
            }
        }
    }
}